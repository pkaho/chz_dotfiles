[edit]
    command = "nvim"

[diff]
    command = "delta"

[merge]
    command = "nvim"
    args = [
        "-d",
        {{ printf "%q" "{{ .Destination }}" }},
        {{ printf "%q" "{{ .Source }}" }},
        {{ printf "%q" "{{ .Target }}" }},
    ]

[cd]
    {{- if eq .chezmoi.os "windows" }}
    command = "pwsh"
    args = "-nologo"
    {{- else }}
    command = "zsh"
    args = "-l"
    {{- end }}
